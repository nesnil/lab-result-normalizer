# 复制为 .env 并按需修改

# 鉴权 Token：留空=不鉴权（仅本地测试）。
# 公网部署务必填一个强随机串（如 openssl rand -hex 32），
# 并在 xskills 平台「新增 MCP 服务器 → 请求头」填：
#   Authorization=Bearer <这里的同一个值>
AUTH_TOKEN=

# 监听地址与端口
HOST=0.0.0.0
PORT=8000
