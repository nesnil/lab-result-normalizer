# MCP 服务（Streamable HTTP transport）
mcp>=1.2.0
uvicorn>=0.30.0
starlette>=0.37.0
pydantic>=2.6.0

# OCR 引擎：RapidOCR（PaddleOCR 模型的 ONNXRuntime 重打包，CPU 即可，无 PaddlePaddle 重依赖）
rapidocr-onnxruntime>=1.3.0
onnxruntime>=1.17.0

# 图像与输入处理
pillow>=10.0.0
numpy>=1.24.0
httpx>=0.27.0

# PDF 化验单 → 图（PyMuPDF，import 名为 fitz）
pymupdf>=1.24.0
